# Google OAuth Configuration
# Get these from https://console.cloud.google.com/
VITE_GMAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
VITE_GMAIL_CLIENT_SECRET=your-client-secret
VITE_GMAIL_REDIRECT_URI=http://localhost:3000/auth/callback

# CopilotKit AI Assistant Configuration
# Get your free public API key from https://cloud.copilotkit.ai
# Leave empty to disable AI assistant features
VITE_COPILOT_API_KEY=
VITE_COPILOT_ENDPOINT=

# API Configuration
# Optional: API base URL for server-side endpoints (SSE, webhooks)
# Defaults to current origin if not specified
VITE_API_URL=

# Upstash Redis Configuration
# Required for SSE (Server-Sent Events) real-time email sync
# Get free Redis at https://upstash.com/
KV_REST_API_URL=https://your-redis.upstash.io
KV_REST_API_TOKEN=your-redis-rest-token

# Optional: Google Cloud Pub/Sub Verification Token
# Used to verify Gmail webhook signatures for security
GOOGLE_PUBSUB_VERIFICATION_TOKEN=

# Optional: Google Cloud Pub/Sub Topic for push notifications
# Used with Gmail webhook integration for real-time email updates
VITE_PUBSUB_TOPIC=projects/your-project/topics/gmail-notifications
