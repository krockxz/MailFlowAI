# Webhook Server Environment Variables

# Port for the webhook server
PORT=8080

# Allowed CORS origins (comma-separated)
# Add your production domain when deployed
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Gmail Pub/Sub Verification Token
# This token is used to verify HMAC signatures on incoming webhook requests
# Obtain from: Google Cloud Console -> Pub/Sub -> Subscription -> Details
# Required for webhook security in production
GOOGLE_PUBSUB_VERIFICATION_TOKEN=your-verification-token-here

# Note: OAuth credentials are configured in the root .env file
# (VITE_GMAIL_CLIENT_ID, VITE_GMAIL_CLIENT_SECRET, VITE_GMAIL_REDIRECT_URI)
